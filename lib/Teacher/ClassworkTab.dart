import 'package:flutter/material.dart';

class ClassworkTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Classwork Content'),
    );
  }
}
